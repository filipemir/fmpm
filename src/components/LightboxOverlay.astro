<div class='overlay' data-lightbox-overlay hidden>
    <div class='frame'>
        <!-- src populated at runtime by lightbox.js; astro:assets needs a build-time-known source, so this can't be optimized -->
        <img
            src=''
            alt=''
        /><!-- allow-raw-img: dynamic lightbox src, see comment above -->
    </div>
</div>

<script src='../scripts/lightbox.js'></script>

<style>
    .overlay[hidden] {
        display: none;
    }
    .overlay {
        position: fixed;
        inset: 0;
        z-index: 90;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 5vh 5vw; /* allow-hardcoded-size: viewport-relative gutter */
        background: var(--scrim);
        cursor: zoom-out;
    }
    .frame {
        width: 100%;
        max-width: var(--width-lightbox-max);
        max-height: 82vh; /* allow-hardcoded-size: viewport-fit constraint */
    }
    .frame img {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: contain;
    }
</style>
