import React from 'react';

import Span from '../components/Span';

export default function HomePage() {
    return (
        <span>
            hiz
            <Span isWelcome={true} />
        </span>
    );
}
