import { css } from '@emotion/core';

const globalCss = css`
    html,
    body {
        background: pink;
        color: white;
        font-family: Roboto, Helvetica, sans-serif;
    }
`;

export default globalCss;
